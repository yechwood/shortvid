plugins { id 'com.android.application' }

android { namespace 'com.shortplay.guard'; compileSdk 35
    defaultConfig { applicationId 'com.shortplay.guard'; minSdk 26; targetSdk 35; versionCode 1; versionName '1.0.0' }
}
